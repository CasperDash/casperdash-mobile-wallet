import { BrowserScreen } from 'screens';
import BrowserRouter from './BrowserRouter';

const { BROWSER_SCREEN } = BrowserRouter;

export const BrowserScreens = {
  [BROWSER_SCREEN]: {
    screen: BrowserScreen,
    title: '',
  },
};
