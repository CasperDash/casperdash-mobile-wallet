const initialState = {
  tokenInfoWithBalance: null,
};

export default function (state = initialState, action = { type: '', payload: '' }) {
  switch (action.type) {
    default:
      return state;
  }
}
